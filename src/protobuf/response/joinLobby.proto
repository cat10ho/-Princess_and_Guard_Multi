syntax = "proto3";

package JoinLobby;

message JoinLobby {
  repeated RoomData rooms = 1;

  message RoomData {
    string roomName = 1;
    uint32 maxPlayers = 2;
    uint32 currentPlayers = 3;
  }
}
